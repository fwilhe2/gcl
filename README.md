# gcl
git clone wrapper with automatic directory layout

By default, repositories are cloned below `~/code`. Set `GCL_BASE_DIR` to use
a different base directory:

```sh
GCL_BASE_DIR=~/src gcl https://github.com/fwilhe2/gcl
```
